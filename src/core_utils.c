#include "core_utils.h"

float maxf(float a, float b) {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}

float minf(float a, float b) {
    if (a < b) {
        return a;
    } else {
        return b;
    }
}